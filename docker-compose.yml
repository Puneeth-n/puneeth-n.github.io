version: '3.8'
services:
  web:
    image: python:3
    working_dir: /site
    volumes:
      - .:/site
    ports:
      - "8000:8000"
    command: python3 -m http.server 8000 --bind 0.0.0.0
    container_name: puneeth-site 